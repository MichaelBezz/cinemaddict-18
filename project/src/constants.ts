export enum AppRoute {
  Main = '/',
  NotFound = '*'
}

export enum APIRoute {
  Films = '/movies',
  Comments = '/comments'
}

export enum Reducer {
  Application = 'APPLICATION',
  Films = 'FILMS',
  Comments = 'COMMENTS'
}

export enum TypeButton {
  Card = 'card',
  Details = 'details'
}
